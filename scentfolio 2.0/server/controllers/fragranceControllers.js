const db = require('../models/fragranceModels');

const fragranceController = {};

fragranceController.addFragrance = (req, res, next) => {
    console.log('made it to add fragrance controller');
    // post request with
};
